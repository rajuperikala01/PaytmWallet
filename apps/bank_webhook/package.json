{
  "name": "bank_webhook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run generate && npx esbuild ./src/index.ts --bundle --platform=node --outfile=api/index.js",
    "generate": "npx prisma generate --schema=../../packages/database/prisma/schema.prisma && mkdir -p ./apps/bank_webhook/node_modules/.prisma && cp -r ./node_modules/.prisma ./apps/bank_webhook/node_modules/",
    "start": "node api/index.js",
    "dev": "npm run build && npm run start",
    "vercel-build": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/express": "^5.0.0",
    "dotenv": "^16.4.5",
    "esbuild": "^0.24.0",
    "express": "^4.21.1",
    "prisma": "^6.1.0"
  }
}
